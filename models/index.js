// Imports the Thought and User models
const User = require("./User");
const Thought = require("./Thought");

// Exports the Thought and User models as properties of an object
module.exports = { User, Thought };
